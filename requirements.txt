# Azure AI Foundry agent hosting (responses protocol)
azure-ai-agentserver-responses==1.0.0b5
azure-ai-agentserver-core==2.0.0b3

# Azure identity
azure-identity==1.25.3

# LangGraph + LangChain
langgraph==1.1.8
langgraph-prebuilt==1.0.10
langchain-core==1.3.0
langchain-azure-ai[opentelemetry]>=1.2.3

# Azure AI Search SDK
azure-search-documents==11.6.0

# FastAPI backend + ASGI server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# MCP server (ticket agent)
mcp[cli]>=1.6.0

# LangChain ↔ MCP adapter (loads MCP tools into LangChain)
langchain-mcp-adapters>=0.1.0

# Local development — load .env file
python-dotenv>=1.0.0
